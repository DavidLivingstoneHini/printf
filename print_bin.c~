#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include "holberton.h"

/**
 * print_bin - prints decimal
 * @b: binary to be printed
 * Return: size
 */

int print_bin(va_list b)
{
n = va_arg(b, int);
return (1);
}
